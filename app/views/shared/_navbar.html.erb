<nav class="navbar navbar-dark fixed-top bg-dark flex-md-nowrap p-0 shadow">
  <a class="navbar-brand col-sm-3 col-md-2 mr-0" href="/">Widgets Manager</a>
  <%= form_tag "/widgets", method: :get do %>
  <%= text_field_tag :q, params[:q], class:"form-control form-control-dark w-100", placeholder: "Search", "aria-label": "Search", type: "text" %>
  <% end %>
  <ul class="navbar-nav px-3">
    <li class="nav-item text-nowrap">
      <% if current_access %>
        <%= link_to "Sign out", sign_out_path, method: :delete, class: "nav-link" %>
      <% else %>
        <a type="button" class="nav-link" data-toggle="modal" data-target="#signInModal">
          Sign in
        </a>
      <% end %>
    </li>
  </ul>
</nav>

<%= render partial: 'shared/sign_in_modal' %>
